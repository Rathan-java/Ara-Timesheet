# Base URL for the backend REST API.
# When set and VITE_USE_MOCK is false, the app will call this URL.
VITE_API_BASE_URL=http://localhost:5000/api

# Use the in-memory mock data layer instead of hitting the backend.
# Set to "false" once the backend is ready.
VITE_USE_MOCK=true
